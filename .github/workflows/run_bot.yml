name: Run Europe Elects Bot

on:
  schedule:
    - cron: '*/30 * * * *' # Runs every 30 minutes
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install Mastodon.py beautifulsoup4 requests

      - name: Run bot
        env:
          MASTODON_ACCESS_TOKEN: ${{ secrets.MASTODON_ACCESS_TOKEN }}
          SCRAPEDO_API_KEY: ${{ secrets.SCRAPEDO_API_KEY }}
        run: python bot.py

      - name: Commit last post ID
        run: |
          git config --global user.name 'github-actions'
          git config --global user.email 'github-actions@github.com'
          git add last_fb_post.txt
          git commit -m "Update last post ID" || echo "No changes to commit"
          git push
